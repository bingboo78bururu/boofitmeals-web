# 부핏Meals QA 리스트

상태: 작성중 / to go / in progress / finished

| # | 항목 | 상태 | 비고 |
|---|---|---|---|
| 1 | 메인페이지 회원·영양코치·운영자 3개 박스 제거 | finished | 퍼블릭 가입은 회원만, 코치/운영자는 계정 직접 발급 |
| 2 | 로그인 후 로고 클릭 시 로그인 상태 유지 | finished | 로고 링크를 "/" → 역할별 홈으로 변경 |
| 3 | 도메인 boofitmeals로 변경 + 로고 텍스트 "부핏Meals"로 변경 | finished | boofitmeals-web.vercel.app, 버핏Meals(2019 원본)와 이름 구분 |
| 4 | 회원가입 페이지 역할 선택란 제거 + 안내 문구 삭제 | finished | 가입 시 자동으로 member 역할 부여 |
| 5 | 로그인 페이지 비밀번호 재설정 기능 | in progress | 기능 구현·배포 완료(/forgot-password, /reset-password), Redirect URL 등록 완료. 실제 재설정 메일 발송/수신 미확인 — 우선순위 보류 중 |
| 6 | 식단 사진 AI 자동 채점 (0~2점) | finished | 원인이었던 Anthropic 계정 크레딧 부족 해결 후 정상 작동 확인 |
| 7 | 하루 세 끼(아침/점심/저녁) 사진 업로드 칸 분리 | finished | 끼니별 독립 인증·저장·AI 채점. 운영자 대시보드 "수행률"은 끼니 수가 아닌 일수 기준으로 재계산 |
| 8 | 사진 업로드 시 에러 발생 (RLS) | finished | 원인: `missions` 테이블에 UPDATE 정책 누락(오늘 미션 재수정/사진 재업로드 시 항상 실패). 정책 추가로 해결 |
| 9 | 당근 캘린더에서 끼니별(아침/점심/저녁) 인증 여부 구분 표시 | finished | 날짜 칸을 세로로 키우고, 점수별(0/1/2점) 점 스타일 차등 + 2점은 초록 하트(SVG, 대칭·레이어 조정 완료) |
| 10 | 영양코치 / 운영자 화면 테스트 | finished | 테스트 완료, 정상 작동 확인 |
| 11 | "목표" / "오늘의 미션"을 뷰 모드·수정 모드로 분리 | finished | 값이 있으면 읽기 전용으로 보여주고 "수정" 눌러야 입력창 등장 |
| 12 | 사진 재업로드로 AI 점수가 바뀌어도 누적 당근이 그대로인 버그 | finished | 원인: "누적 당근"이 점수가 아니라 인증 횟수(count)로 계산되고 있었음. 끼니별 AI 점수의 합(sum)으로 재정의 — 점수 0점이면 그 끼니는 당근 0개로 반영됨 |
| 13 | 미션 인증 직후 채점 결과 팝업 (당근 N개 안내) | finished | 인증 성공 시 AI 점수(0/1/2)에 맞는 메시지가 팝업으로 즉시 표시 |
| 14 | 오늘의 미션 — 날짜 단위 조회/이동 UI (오늘 ±3일 스트립, ‹/›로 더 과거 이동) | finished | 오늘만 입력 가능, 과거는 조회만(수정 불가), 미래는 비활성화. `/member?date=YYYY-MM-DD` |
| 15 | 서버가 "오늘" 날짜를 UTC 기준으로 인식해서 한국 시간과 어긋나는 버그 | finished | Vercel 서버는 UTC로 동작 — 자정~오전 9시 사이 한국은 이미 다음날인데 서버는 전날로 계산했음. 모든 "오늘" 계산을 Asia/Seoul 기준으로 통일 |
| 16 | 당근 캘린더에 지난달/다음달 이동 UI 추가 | finished | 상단 ‹ YYYY년 M월 › 로 월 이동, 이전/다음달 여백은 흐린 숫자로 표시(참고 이미지 스타일) |
| 17 | 오늘의 미션 날짜 스트립 클릭 시 반응 느림 (모바일 피드백 부재) | finished | 탭 시 눌림 효과(scale+배경색) 추가, 페이지 전환 중엔 날짜 숫자 대신 점 3개 로딩 표시(Next.js useLinkStatus) |
| 18 | 영양코치가 AI 채점 점수를 직접 조정(override)할 수 있는 기능 | finished | `missions.coach_score` 컬럼 추가, 있으면 최종 점수로 우선 적용(coach_score ?? ai_score). 코치 화면 피드에서 0/1/2 버튼으로 조정 + "AI 점수로 되돌리기". DB 트리거로 회원은 note/photo/ai_score만, 코치는 coach_score만 수정 가능하도록 컬럼 단위 제한. 랭킹·캘린더·오늘의미션 당근 집계 모두 최종 점수 기준으로 반영 |
| 19 | 당근 캘린더 하단 문구 "YYYY년 M월 n일 실천했어요" → "이번달 n일 실천했어요"로 변경 | finished | |
| 20 | 오늘의 미션 — 식단 사진 업로드 칸이 프레임 밖으로 튀어나가는 레이아웃 깨짐 | finished | 파일 입력 줄바꿈 허용 + 너비 제한/말줄임 처리, 미션 카드에 overflow-hidden 추가 |
| 21 | 회원가입 3단계 분리 (계정정보 → 영양코치 선택 → 클래스 선택) + 클래스 개념 도입, 랭킹보드는 같은 클래스끼리만 | finished | `/signup` → `/signup/coach` → `/signup/class` → `/member`. 각 단계 "나중에 선택할게요" 건너뛰기 가능. `classes` 테이블 추가, 운영자 화면(`/admin`)에 클래스 생성 UI 추가. 랭킹보드는 `profiles.class_id` 기준으로 스코프, 클래스 미배정 회원은 안내 문구 표시 |
| 22 | 영양코치가 피드백 남길 때 "duplicate key ... feedback_mission_id_key" DB 에러 노출 | finished | 원인: 피드백은 `insert`만 가능해서 이미 피드백이 있는 미션에 다시 남기면 그대로 에러. `upsert`로 변경 + 코치가 자기 피드백을 수정할 수 있도록 뷰/수정 모드 토글 추가(목표·오늘의미션과 동일한 패턴). DB에 feedback UPDATE 정책 추가 필요 |
| 23 | 회원가입 온보딩에서 코치 선택 시 "new row violates row-level security policy for table coach_assignments" 에러 | finished | 원인: `coach_assignments` insert/update 권한이 운영자 전용이라, 회원 본인이 온보딩에서 코치를 선택하는 새 기능과 충돌. 회원이 본인 배정만(실제 코치 계정에 한해) insert/update 할 수 있는 정책 추가 |
| 24 | 회원가입 시 이미 등록된 이메일이면 영어 에러("User already registered") 노출 | finished | "이미 등록된 회원입니다."로 한글 메시지 표시 |
| 25 | 회원가입 시 입력하는 "이름"을 고유값으로 강제 + 화면 표기를 "닉네임"으로 변경 | finished | `profiles.name` unique 제약 추가, 가입 전(비로그인) 중복 확인용 `name_taken` DB 함수 추가. 중복이면 "이미 사용 중인 닉네임이에요." 표시 |
| 26 | 유저 화면 하단 탭바 도입 (피드/랭킹보드/홈/당근캘린더/마이페이지 5개) | finished | 참고 이미지 스타일로 중앙(홈) 아이콘만 살짝 떠 있는 원형 버튼으로 강조. 홈은 기존 메인화면(인사말·누적당근·오늘의미션)만 남기고 목표설정은 마이페이지로 이동. 새 화면: `/member/feed`(같은 클래스 동료 미션 피드, 코치 화면과 동일 구조의 읽기 전용), `/member/mypage`(목표·담당코치·클래스를 그 자리에서 바로 변경, 온보딩과 달리 리다이렉트 없이 저장). 클래스 동료끼리 미션/피드백을 볼 수 있도록 RLS 정책 추가 |
| 27 | 마이페이지 담당코치·클래스 설정도 뷰/수정 모드 토글로 변경 | finished | 선택되어 있으면 읽기 전용으로 이름만 보여주고 "수정하기" 눌러야 선택 목록이 뜸 (목표·오늘의미션과 동일한 패턴) |
| 28 | 마이페이지 목표 "수정" 버튼을 클래스/코치 설정처럼 텍스트 버튼으로 통일 | finished | 박스 테두리 있는 버튼 → 박스 우측 하단의 텍스트 링크 "수정하기"로 변경 |
| 29 | 상단바 바로 밑에 "내 목표: 현재→목표까지 N일 남았어요" 공지 바 추가 (중요도 상) | finished | 회원 전용, 모든 회원 화면에 공통 노출. 목표 미설정 시 마이페이지로 유도하는 문구 표시 |
| 30 | 홈에 오늘의 체중/체지방률 입력칸 추가 (중요도 상) | finished | `body_logs` 테이블 신설(하루 1건, 그래프용 시계열 데이터). 체중·체지방률 둘 다 선택 입력, 뷰/수정 모드 토글 |
| 31 | 유저 피드에 코치 피드백뿐 아니라 AI 채점 결과도 표시 (중요도 중) | finished | 코치 화면과 동일하게 "🤖 AI 채점 N점 · 사유" 배지 추가, 코치가 점수를 조정한 경우 "🥕 최종 점수 (코치 조정)" 배지도 함께 표시 |
| 32 | 마이페이지 목표 단위를 체지방률 외에 체중도 선택 가능하게 (중요도 중) | finished | `goals` 테이블에 `unit`(체지방률/체중) 컬럼 추가, 기존 `current_body_fat`/`target_body_fat`를 범용 `current_value`/`target_value`로 이름 변경. 목표 박스·공지 바 모두 선택한 단위(%/kg)에 맞춰 표시 |
| 33 | 하단 메뉴 명칭 변경: 홈→인증, 랭킹보드→랭킹, 마이페이지→MY (중요도 하) | finished | "피드"·"당근캘린더"는 그대로 유지 |
| 34 | "피드" 메뉴명을 더 직관적으로 (중요도 하) | finished | 아이디에이션 후 "우리반"으로 결정 및 적용 |
| 35 | 랭킹보드를 다른 메뉴에 합치고 2번째 메뉴에 목표 그래프 (중요도 하) | finished | 랭킹보드는 "우리반" 상단에 TOP 5 미니 리스트로 통합(단독 페이지 제거). 빈 2번째 자리에 "성장" 탭 신설 — `body_logs` 기반 체중/체지방률 추이 그래프(목표선·크로스헤어·표 보기 포함) |
| 36 | 체지방률 새로 입력해도 상단바 텍스트가 갱신 안 되는 버그 (중요도 상) | finished | 원인: 상단바·마이페이지 목표의 "현재" 값이 `goals.current_value`(가입 시 스냅샷)만 보고 있었음. 홈에서 기록한 `body_logs`의 가장 최근 값을 우선하도록 수정 |
| 37 | 목표 단위에 근육량 추가 (중요도 중) | finished | `goal_unit`에 `muscle_mass_kg` 추가, `body_logs`에도 근육량 컬럼 추가. 홈 오늘의 기록 입력칸도 3개(체중/체지방률/근육량)로 확장 |
| 38 | 상단바 계속 floating 고정 (중요도 하) | finished | 헤더+목표 공지 바를 `sticky top-0`으로 묶어서 스크롤해도 상단에 고정 |
| 39 | 트레이너 피드백 입력 시 "new row violates row-level security policy (USING expression) for table feedback" 에러 (중요도 상) | finished | 원인: 피드백 UPDATE 권한이 원작성 코치 본인으로만 제한돼 있어서, 담당 코치가 바뀌었거나 다른 코치 계정이면 기존 피드백을 못 고침. 현재 담당 코치면 이전 작성자와 무관하게 수정 가능하도록 정책 추가 |
| 40 | 상단바 투명도 0 (완전 불투명) (중요도 중) | finished | 목표 공지 바의 `/20`, `/60` 알파 배경을 solid 색상으로 변경 |
| 41 | 목표 달성 시 상단바 문구를 "목표를 달성했어요! 축하해요!"로 (중요도 중) | finished | 체지방률/체중은 현재≤목표, 근육량은 현재≥목표일 때 달성으로 판정. 달성 시 초록 배경으로 전환 |
| 42 | 오늘의 미션/피드의 AI 채점 결과 배지가 pill(rounded-full) 모양이라 텍스트가 밖으로 나가는 것처럼 보임 (중요도 하) | finished | 피드·코치 화면의 배지를 `rounded-full` → `rounded-lg`로 통일 (오늘의 미션 화면은 이미 rounded-lg였음) |
| 43 | 메뉴명 변경: 성장→내 변화, 인증→기록 (중요도 하) | finished | |
| 44 | 마이페이지 목표 수정 시 현재/목표 수치 입력칸이 오른쪽으로 튀어나가는 현상 (중요도 하) | finished | 원인: number/date input에 `w-full` 누락(파일 업로드 칸 때와 동일한 원인). 목표·오늘의 기록 입력 폼 모두 수정 |
| 45 | "내 변화" 그래프가 기록 1건뿐이어도 뜨게 (목표=수평선, 현재=점) (중요도 중) | finished | 기존 컴포넌트가 이미 점 1개도 지원해서 페이지의 "2건 미만이면 안내문구" 조건만 "1건 미만"으로 완화 |
| 46 | 그래프 안 숫자가 좁게(찌그러져) 보이는 현상 (중요도 하) | finished | 원인: `viewBox`(가로:세로 600:220)와 실제 렌더 크기(모바일 좁은 폭·고정 높이)가 안 맞는데 `preserveAspectRatio="none"`으로 강제로 채우면서 텍스트·점이 가로로 눌림. SVG에 `aspect-ratio` CSS를 줘서 항상 viewBox 비율대로만 렌더되도록 수정 |
| 47 | 마이페이지에서 처음 입력한 체지방률(현재 수치)이 "내 변화" 그래프에 안 나오는 현상 (중요도 상) | finished | 원인: 목표 저장 시 `goals` 테이블에만 기록되고 그래프 데이터인 `body_logs`엔 안 남았음. 이제 목표의 "현재" 수치를 저장할 때마다 오늘 날짜 기록으로도 함께 남도록 수정 (단, 이미 지나간 과거에 입력했던 최초 30%는 그때 기록을 안 남겼어서 소급 복구는 안 됨 — 마이페이지에서 다시 저장하면 오늘 날짜로 반영됨) |
| 48 | 기록 탭/마이페이지에서 체중·체지방률 입력해도 이전 수치가 사라지고 최근 값만 남는 현상 (중요도 상) | finished | 확인 결과 버그 아님 — `body_logs`가 "하루에 하나" 설계라서, 오늘 안에 기록 탭과 마이페이지 양쪽에서 입력하면 같은 날짜 칸에 덮어써지는 게 정상 동작. 사용자 확인 후 현재 방식 유지로 결정. 진단용으로 추가했던 로그만 정리 |
| 49 | 그래프 숫자가 너무 작음 — 가로로 긴 빈 공간은 자르고 데이터 있는 부분을 확대 (중요도 중) | finished | 폰트 크기 확대(11→15~16), 점이 적을수록(특히 1개) 차트 가로 폭 자체를 좁혀서 렌더 — 데이터가 적을 때 카드 전체 너비에 점 하나만 덩그러니 뜨던 걸 개선. "내 변화" 페이지에 남아있던 "성장" 제목 잔재도 "내 변화"로 통일 |
| 50 | 마이페이지 목표 단위(체지방률/체중/근육량) 선택 칸 폰트가 두 줄로 넘어감 (중요도 하) | finished | 폰트 크기 축소(text-sm→text-xs) + `whitespace-nowrap`으로 한 줄 유지 |
| 51 | 오늘의 체중/체지방률 입력 박스를 "기록" 탭에서 "내 변화" 탭으로 이동, 그래프 카드 우측 하단에 "수정하기" 버튼으로 열고 닫기 (중요도 중) | finished | "기록" 탭에서는 완전히 제거. "내 변화" 카드 하단에 뷰/수정 토글 추가 — 기록 없으면 기본으로 입력폼이 열려있고 버튼은 "기록하기", 있으면 그래프만 보이고 "수정하기" 눌러야 입력폼 등장 |
| 52 | "내 변화" 수정 버튼을 "오늘 수치 기록하기" CTA 버튼으로 강조 + 메뉴명 '기록'→'식단인증' (중요도 하) | finished | 우측 하단 작은 텍스트 링크 → 그래프 카드 바깥 하단에 가로로 긴 주황색 버튼("오늘 수치 기록하기")으로 변경. 하단 메뉴 '기록'을 '식단인증'으로 변경 |
| 53 | 상단바 유저이름·role 사이 가운뎃점(·) 제거 (중요도 하) | finished | "김유저 · 회원" → "김유저 회원" |
| 54 | 회원가입 코치 선택 UI를 2x2 카드 그리드로 개편 + 코치별 해시태그(예: 여성다이어트전문, 벌크업) | finished | 코치가 자기 페이지(`/coach`)에서 한줄 소개·해시태그를 직접 입력하는 "내 프로필" 섹션 추가. `profiles.bio`/`profiles.tags` 컬럼 신설. `/signup/coach`는 라디오 리스트 → 2열 카드(자리표시 아이콘+이름+소개글+해시태그)로 변경. 프로필 사진 업로드는 추후 과제로 보류, 지금은 이니셜 아이콘 |
| 55 | 영양코치 페이지에 프로필 사진 업로드 칸 추가 | finished | `profile-photos` Storage 버킷 신설(공개 읽기, 본인 폴더에만 업로드), `profiles.photo_url` 컬럼 추가. "내 프로필"에서 사진 업로드 시 회원가입 코치 선택 카드(2x2)에도 실제 사진이 반영됨(없으면 이니셜 아이콘 유지) |
| 56 | 코치 프로필 사진 업로드 시 "이 페이지를 불러올 수 없음" 에러 | finished | 원인: Vercel 서버리스 함수는 요청 본문이 4.5MB로 하드 제한인데, 사진 용량 체크를 5MB까지 허용해놔서 그 사이 크기는 앱에 도달하기도 전에 인프라 단에서 연결이 끊김(서버 로그도 안 남음). 코치 프로필·오늘의 미션 사진 업로드 모두 4MB 제한으로 낮추고, `next.config.ts`의 bodySizeLimit도 4.5mb로 현실화 |
| 57 | 코치 프로필 저장 시 사진 업로드 실패하면 같이 입력한 소개글/해시태그도 저장 안 되는 버그 | finished | 원인: 사진 업로드 에러 시 즉시 return 해버려서 그 앞에서 처리해야 할 소개글/해시태그 저장까지 통째로 스킵됨. 소개글/해시태그를 사진 업로드보다 먼저, 독립적으로 저장하도록 순서 변경 |
| 58 | 회원가입 2단계를 "영양코치 선택" 대신 "목표 선택(감량/증량)"으로 변경 | finished | `/signup/coach` 삭제, `/signup/goal`로 대체 — "체중/체지방률 감량" 선택 시 목표 단위 body_fat_pct, "근육량 증가" 선택 시 muscle_mass_kg로 목표(단위만) 미리 생성. 코치 선택은 온보딩에서 빠지고 마이페이지에서만 가능 — 대신 마이페이지 코치 선택 UI를 2x2 카드(사진·소개글·해시태그)로 업그레이드해서 기존 작업 유지. 부수적으로 발견한 버그 수정: 목표가 "단위만 있고 수치는 비어있는" 상태일 때 뷰 모드가 "null%"로 깨지던 것 수정 |
| 59 | signup/goal, signup/class의 "나중에 선택할게요" 제거 + 이전 페이지로 돌아가는 화살표 추가 | finished | 하단 건너뛰기 링크 삭제, 상단 단계 표시가 "← 2/3", "← 3/3" 형태의 뒤로가기 링크로 변경 |
| 60 | 상단바·"내 변화" 목표 미설정 안내의 "마이페이지에서 설정하기"를 "설정하기>"로 축약 (중요도 하) | finished | |
| 61 | 당근캘린더 안내 문구 "미션을 인증한 날마다 당근이 채워져요" → "식단을 인증해서 당근을 채워보세요."로 변경 (중요도 하) | finished | |
| 62 | 당근캘린더 우측 상단에 당근 0/1/2개 범례(회색 dot/주황 dot/초록하트+주황 dot) 표시 | finished | 실제 캘린더에서 쓰는 `CarrotDot`을 그대로 재사용해서 표기 일관성 유지 |
| 63 | "설정하기>" 링크 오른쪽 정렬 (중요도 하) | finished | 상단바 목표 미설정 안내, "내 변화" 목표 미설정 안내 둘 다 문구는 좌측·링크는 우측으로 정렬 |
| 64 | 마이페이지 담당 영양코치 선택 기능 제거 → "식단 목적"(감량/증량) 설정으로 대체, 그 밑에 현재/목표 수치·목표일을 "하위 목표"로 배치 (중요도 중) | finished | 코치 배정은 이제 운영자만 가능(마이페이지에서도 온보딩에서도 회원이 직접 고를 수 없음) — 코치 카드 UI는 폐기. "식단 목적" 변경 시 단위가 실제로 바뀌면 하위 목표(현재/목표 수치·목표일)는 의미 없어지므로 자동 초기화. `GoalForm`은 더 이상 단위를 직접 고르지 않고 "식단 목적"이 정한 단위를 그대로 받아 표시 |
| 65 | AI 채점 기준을 회원의 식단 목적(감량/증량)에 따라 다르게 적용 (중요도 중) | finished | 감량이면 저탄수·채소·단백질 위주 구성을, 증량이면 단백질+충분한 총 칼로리(탄수화물 포함) 구성을 기준으로 평가하도록 프롬프트 분기. 미션 인증 시점의 목표 단위를 조회해서 적용하며, 이후 목적이 바뀌어도 이미 채점된 과거 점수는 재채점되지 않음(과거 점수를 바꾸는 건 코치의 점수 조정만 가능, 기존 구조 그대로) |
| 66 | 마이페이지 "하위 목표"를 "나의 목표"로 변경 | finished | |
| 67 | 첫 화면 헤드라인 "뚜렷한 목표를 세우고, 매일의 실천을 당근으로 쌓아요" 폰트 크기 축소 (모바일에서 4줄 → 3줄로) | finished | 모바일 기준 text-4xl → text-3xl (데스크톱 sm: 이상은 기존 text-5xl 유지) |
| 68 | 첫 화면 헤드라인 문구를 "뚜렷한 목표를 세우고, 매일 당근을 쌓아보세요"로 변경 | finished | |
| 69 | 식단인증(사진 업로드) 시 용량이 작아도(2.6MB) "이 페이지를 불러올 수 없음" 에러 | finished | 원인: 사진 업로드 + 목표 조회 + AI 채점(Anthropic API 호출)이 합쳐지면서 Vercel 서버리스 함수 기본 실행 제한(10초)을 넘겨 강제 종료(에러 로그도 안 남음). `/member` 페이지에 `maxDuration = 30` 설정해서 여유 확보 |
| 70 | 특정 용량대(2.6MB, 3.5MB) 사진만 업로드 실패, 더 작은 사진은 정상 | finished | 서버 쪽 용량 임계값을 계속 추측하는 대신 근본 해결 — 사진을 서버로 보내기 전에 브라우저에서 미리 리사이즈(최대 1280px)·JPEG 재압축해서 전송하도록 변경(오늘의 미션·코치 프로필 사진 둘 다 적용). 실패 시(구형 브라우저 등) 원본 그대로 전송하는 안전한 폴백 포함 |
| 71 | 모든 화면 전환 시 로딩중 표시 | finished | Next.js `loading.tsx` 컨벤션으로 회원(홈/우리반/내변화/식단인증/당근캘린더/마이페이지)·코치·운영자·회원가입(목표/클래스 선택) 라우트에 로딩 스피너 추가. 상단바·하단탭은 그대로 유지되고 콘텐츠 영역만 로딩 표시로 바뀜 |
| 72 | 로딩 동그라미를 상단바·하단바 제외한 화면 가운데(위아래 기준)로 정렬 (중요도 하) | finished | `<main>`을 flex column으로 바꿔서 로딩 컴포넌트가 콘텐츠 영역 전체 높이를 채우고 그 안에서 수직 중앙 정렬되도록 수정 |
| 73 | 신규 회원가입 시 담당 영양코치를 자동으로 "김부핏"으로 배정 (중요도 상) | finished | 가입 직후 '김부핏'이라는 이름의 코치 계정을 찾아 자동 배정. 해당 이름의 코치가 없으면 조용히 건너뜀(가입 자체는 실패하지 않음) |
| 74 | 목표(체지방률/체중/근육량) 및 오늘의 기록 입력 시 음수 입력 방지 (중요도 중) | finished | 입력창에 `min="0"` 추가 + 서버 액션(saveGoal, logBody)에도 0 미만이면 거부하는 검증 추가 |
| 75 | "내 변화"에서 오늘 수치 기록 시, 목표 단위가 근육량이면 근육량 칸만, 체중이면 체중 칸만 보이게 (중요도 중) | finished | 기존엔 체중/체지방률/근육량 3칸이 항상 다 보였음 — 이제 회원의 현재 목표 단위에 해당하는 입력칸 1개만 표시 |
| 76 | 상단 닉네임도 역할과 같은 오렌지색으로 변경 (중요도 하) | finished | "닉네임 · 역할"에서 닉네임만 회색이었던 걸 역할과 동일한 색으로 통일 |
| 77 | "내 변화" 그래프 안 굵은 주황색 숫자 제거 (상단 수치와 중복 표시 방지) | finished | 그래프 점 옆에 떠 있던 "끝점 라벨"(굵은 주황 텍스트) 삭제 — 같은 수치는 카드 상단에만 한 번 표시 |
| 78 | 하단 메뉴명 띄어쓰기 수정: '우리반'→'우리 반', '식단인증'→'식단 인증' | finished | |
| 79 | 상단 역할 폰트 굵기를 닉네임과 완전히 동일하게 | finished | 역할 라벨에 있던 font-medium(굵게) 제거 |
| 80 | 회원가입 버튼 클릭 시 온보딩 튜토리얼(이미지/텍스트 슬라이드) 노출 | finished | 랜딩 페이지 "회원가입"/"무료로 시작하기" 버튼을 눌렀을 때 3장짜리 카드 슬라이드 모달 표시 → "시작하기"를 눌러야 /signup으로 이동하던 기능. **이후 98번에서 로그인 후 튜토리얼로 방향 전환하면서 이 기능은 제거됨** |
| 81 | 튜토리얼을 실제 앱 스크린샷 기반 이미지로 교체 + 4번째 슬라이드에 당근 정책(하루 최대 6개, 월 100개↑ 전액환급, 문구만) 추가 | finished | 80번(대상 기능)이 98번에서 제거되면서 이 작업은 더 이상 필요 없음. 스크린샷 기반 교체가 필요하면 98번(로그인 후 튜토리얼) 대상으로 다시 요청 필요 |
| 82 | 식단 인증 시 뜨는 AI 채점 결과 팝업 문구를 제공된 스크린샷 3종과 동일하게 수정 (줄바꿈 포함) | finished | 0/1/2점 팝업 텍스트를 스크린샷 그대로로 교체, 두 줄 렌더링을 위해 whitespace-pre-line 적용 |
| 83 | PC에서 로그아웃에 커서 갖다대도 손가락 커서(pointer)가 안 뜸 (중요도 하) | finished | Tailwind preflight가 button 기본 cursor를 default로 리셋하는 문제 — cursor-pointer 클래스 추가 |
| 84 | 회원 메인페이지 식사 인증 버튼 3개 텍스트 줄바꿈 방지 (폰트 축소) (중요도 하) | finished | 버튼 텍스트 text-sm으로 축소 + whitespace-nowrap 추가 |
| 85 | 회원 메인페이지 날짜 UI 위에 월(예: 7월) 표시, 당근캘린더 월 폰트 크기로 | finished | 날짜 스트립 위에 `{N}월` 라벨 추가 (text-lg font-bold, 캘린더 페이지 월 표시와 동일 스타일) |
| 86 | AI 채점 기준에 탄단지 구성뿐 아니라 전체 음식의 양도 반영 (구성 좋아도 양이 터무니없이 적으면 1점) | finished | 초밥 3피스처럼 구성은 좋지만 양이 한 끼로 보기 부족한 경우 2점을 줄 수 없도록 채점 프롬프트에 양 기준 추가 |
| 87 | API 연동하는 모든 부분에서 로딩화면을 불투명 오버레이로 처리해서 다른 버튼을 못 누르게 수정 | finished | 공용 `LoadingOverlay` 컴포넌트(전체화면 고정 + 90% 불투명 배경 + 스피너) 신설, 서버 액션을 쓰는 폼 16곳(식단 인증, 목표/기록 저장, 코치 피드백/점수조정/프로필, 회원가입/로그인/비밀번호 재설정, 온보딩, 관리자 클래스/코치배정 등) 전체에 pending 상태와 연동해 적용 |
| 88 | 상단 목표 배너 "내 목표: 수치→수치" 앞에 어떤 수치인지(체지방률/체중/근육량) 표시 | finished | "🎯 내 목표: 체지방률 27%→25% · 4일 남았어요" 형태로 goalUnitLabel 추가 |
| 89 | 식단 인증 시 사진을 선택이 아닌 필수로 변경 | finished | 최초 인증(기존 사진 없음)일 때 file input에 required 추가 + 서버 액션에도 기존 사진이 없는데 새 사진도 없으면 거부하는 검증 추가(클라이언트 우회 방지). 이미 사진이 있는 미션을 메모만 수정할 때는 재업로드 불필요 |
| 90 | 당근캘린더 메뉴 아이콘을 당근으로, 메뉴명을 '당근 현황'으로 변경 | finished | 하단 네비 아이콘을 캘린더 모양에서 당근 라인 아이콘(CarrotIcon, 기존 CalendarIcon 대체)으로 교체, 라벨/페이지 제목 모두 '당근 현황'으로 통일 |
| 91 | 캘린더 하단 문구를 '이번달 n일 실천했어요' → '이번달 총 당근 n개 모았어요. 100개 되면 등록비용 전액 환급' 으로 변경 | finished | 날짜 수 대신 이번달 획득한 당근 총합(finalScore 합산)을 표시하고 100개 환급 안내 문구 추가 (문구만, 실제 결제/환급 로직 없음) — 이후 92번에서 상단 서브텍스트로 위치 이동 |
| 92 | 당근현황 상단 서브텍스트를 "환급챌린지 도전!(줄바꿈)이번 달 당근이 100개가 되면 비용 전액 환급해드려요!"로 교체 / 캘린더 아래 기존 텍스트 삭제 / 캘린더 아래 주황색 '당근 받으러 가기' 버튼(→ /member) 추가 / 식단인증 페이지의 누적 당근 박스를 당근현황에도 표시 | finished | 누적 당근 박스를 `CarrotCount` 공용 컴포넌트로 추출해 두 페이지에서 재사용, 당근현황에도 전체 미션 합산 쿼리 추가 |
| 93 | 당근현황 서브텍스트를 "당근 100개 모으면 전액 환급!"으로 축약 | finished | 92번의 2줄짜리 문구를 한 줄로 줄임 |
| 94 | 트레이너(코치) 화면 피드가 미션 인증한 시간 최신순으로 뜨게 변경 | finished | 정렬 기준을 `mission_date`(날짜만) → `created_at`(실제 인증 타임스탬프) 내림차순으로 변경. 같은 날짜의 여러 미션도 실제 인증 순서대로 정렬됨 |
| 95 | 어드민 화면에서 기존 클래스 삭제 기능 추가 | finished | `classes delete by admin` RLS 정책은 이미 있었음(미사용 상태) — 클래스 목록 각 행에 확인창(confirm)을 거치는 삭제 버튼 추가. 삭제 시 배정된 회원은 FK `on delete set null`로 자동 미배정 처리 |
| 96 | 어드민 화면 전체 개편 — 운영자의 4가지 역할(①AI/코치 모니터링용 전체 피드 ②회원 클래스·코치 배정 조정 ③환급챌린지용 회원별 이번달 당근 확인 ④회원·코치 닉네임/이메일/미션내용 확인)을 반영 | finished | (1) "전체 미션 피드" 섹션 신설 — 전체 회원의 최근 미션 40건을 담당코치·AI채점·코치조정점수·코치피드백 유무와 함께 표시. (2) 클래스 관리에 회원별 클래스 재배정 폼(`ReassignClassForm`) 추가, 기존 코치 배정 폼은 유지. (3)(4) "회원 · 코치 디렉토리" 표 신설 — 닉네임/역할/이메일/클래스/담당코치/이번달 누적 당근(100개 이상 시 "환급 대상" 하이라이트)을 한 번에 확인. 이메일은 `auth.users`에만 있어 `admin_user_directory()` security definer 함수(운영자만 결과 반환)로 노출, 회원 클래스 직접 변경을 위해 `profiles update by admin` RLS 정책 추가 — **`supabase/admin-directory.sql`을 Supabase SQL Editor에서 실행해야 디렉토리 표와 클래스 재배정이 동작함** |
| 97 | 어드민 화면을 3개 페이지로 분리: 대시보드(/admin) · 미션 피드(/admin/feed) · 회원/코치 관리(/admin/users) + 대시보드에 이번달 누적당근 분포(20개 단위, 80~99개 구간 연한 빨간색 강조) 추가 | finished | 상단 네비에 "미션 피드"/"회원/코치 관리" 링크 추가. 96번에서 한 페이지에 몰려있던 섹션들을 역할별로 재배치: 대시보드=통계+수행률분포+AI채점분포+당근분포(신규), 미션 피드=전체 미션 모니터링, 회원/코치 관리=디렉토리+클래스 관리+코치 배정. 컴포넌트(AssignCoachForm 등)는 /admin/users로 이동 |
| 98 | 회원가입 전 튜토리얼(80번) 제거 → 회원이 로그인하면 뜨는 튜토리얼로 전환 | finished | 랜딩페이지 "회원가입"/"무료로 시작하기" 버튼을 다시 일반 링크로 되돌리고 `SignupTutorialButton` 컴포넌트 삭제. 대신 `login()` 서버 액션에서 회원 로그인 성공 시 `/member?welcome=1`로 리다이렉트하고, `/member` 페이지가 이 파라미터를 보고 `MemberWelcomeTutorial` 모달을 표시. 모달을 닫으면 `/member`로 URL을 정리해서 새로고침해도 다시 뜨지 않음. 로그인할 때마다(매번) 노출되는 사양 — **99번에서 슬라이드 카드 방식을 스포트라이트 방식으로 전면 교체** |
| 99 | 98번의 카드 슬라이드 튜토리얼을 스포트라이트(코치마크) 방식으로 교체 — 화면을 어둡게 덮고 대상 요소만 밝게 강조 | finished | 5단계: ①상단 목표배너 "설정하기>" ②하단 "식단 인증" 탭 ③"당근 현황" 탭 ④"우리 반" 탭 ⑤"내 변화" 탭을 순서대로 강조하고 옆에 말풍선 텍스트+"다음/확인" 버튼 표시. `getBoundingClientRect()`로 대상 위치를 측정해 테두리 박스(box-shadow로 나머지 영역만 어둡게 처리)를 그리는 방식, 대상이 화면에 없으면(예: 이미 목표를 설정해 배너 문구가 달라진 경우) 자동으로 다음 단계로 건너뜀. 대상 요소에 `id="tour-goal-setting"`(goal-banner.tsx), `id="bottom-nav-*"`(member/layout.tsx bottomNav) 추가. **2~5단계 안내 문구는 초안이라 확정 문구로 다듬어야 함** |
| 100 | 식단인증 메모 입력창(모바일) 우측 하단 리사이즈 핸들로 창 크기가 늘었다 줄었다 하는 것 방지 | finished | textarea에 `resize-none` 추가. 코치 피드백 입력창에도 동일한 핸들이 있는데, 이번 요청 범위(식단인증 화면)에만 적용함 |

> 새 QA 요청 주시면 이 표에 to go로 추가하고, 진행 상태 바뀔 때마다 업데이트할게요.
